# Project name and version
project(CTEST_SCALAR_CONSERVATION_FE_2D VERSION 1.0)
message(STATUS "")
message(STATUS "===== compiling project ${PROJECT_NAME} =====")

# List of tests
#=== FE_1D_LINEAR_TRANSPORT_TRANSLATION ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "FE_1D_LINEAR_TRANSPORT_TRANSLATION"  NB_REGEX 2
  INCLUDE_TEMPLATE ON FE_DIM 1D LIST_N_PROCS ${LIST_PROC}
)

#=== FE_2D_LINEAR_TRANSPORT_ROTATION ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "FE_2D_LINEAR_TRANSPORT_ROTATION"  NB_REGEX 2
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== FE_2D_LINEAR_TRANSPORT_TRANSLATION ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "FE_2D_LINEAR_TRANSPORT_TRANSLATION"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)
