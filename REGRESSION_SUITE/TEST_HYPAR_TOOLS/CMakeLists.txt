# Project name and version
project(CTEST_HYPAR_TOOLS VERSION 1.0)
message(STATUS "")
message(STATUS "===== compiling project ${PROJECT_NAME} =====")

# List of tests
#=== REFINEMENT_P1 ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "REFINEMENT_P1"  NB_REGEX 1
  INCLUDE_TEMPLATE OFF FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== REFINEMENT_P2 ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "REFINEMENT_P2"  NB_REGEX 1
  INCLUDE_TEMPLATE OFF FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== 1D_P2_DIRICHLET ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "1D_P2_DIRICHLET"  NB_REGEX 3
  INCLUDE_TEMPLATE OFF FE_DIM 1D LIST_N_PROCS ${LIST_PROC}
)

#=== 1D_P3_DIRICHLET ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "1D_P3_DIRICHLET"  NB_REGEX 3
  INCLUDE_TEMPLATE OFF FE_DIM 1D LIST_N_PROCS ${LIST_PROC}
)