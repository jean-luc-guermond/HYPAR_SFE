# Project name and version
project(CTEST_HYPAR_TOOLS VERSION 1.0)
message(STATUS "")
message(STATUS "===== compiling project ${PROJECT_NAME} =====")

# List of tests
#=== 1D_REFINEMENT_FACTOR_4_P3 ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "1D_REFINEMENT_FACTOR_4_P3"  NB_REGEX 2
  INCLUDE_TEMPLATE ON FE_DIM 1D LIST_N_PROCS ${LIST_PROC}
)

#=== 2D_REFINEMENT_FACTOR_2_P1 ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "2D_REFINEMENT_FACTOR_2_P1"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== 2D_REFINEMENT_FACTOR_2_P2 ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "2D_REFINEMENT_FACTOR_2_P2"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== 2D_REFINEMENT_FACTOR_4_P1 ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX ${MAX_PROC_PARALLEL_TEST} DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "2D_REFINEMENT_FACTOR_4_P1"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)
