# Project name and version
project(CTEST_EULER_FE_2D VERSION 1.0)
message(STATUS "")
message(STATUS "===== compiling project ${PROJECT_NAME} =====")

# List of tests
#=== FE_2D_ISENTROPIC_VORTEX ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "FE_2D_ISENTROPIC_VORTEX"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== FE_2D_WIND_TUNNEL ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 4
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "FE_2D_WIND_TUNNEL"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)

#=== FE_2D_ISENTROPIC_VORTEX_HIGH_ORDER ===#
generate_random_procs_list(LIST_PROC 
MIN 1 MAX 16 DEFAULT 1
LEVEL ${PARALLEL_TEST_LEVEL} LIST_LEVEL (1 3 7 13))
add_regression_test(
  TEST_NAME "FE_2D_ISENTROPIC_VORTEX_HIGH_ORDER"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)
add_regression_test(
  TEST_NAME "FE_2D_ISENTROPIC_VORTEX_HIGH_ORDER_LIMITING"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)
add_regression_test(
  TEST_NAME "FE_2D_ISENTROPIC_VORTEX_HIGH_ORDER_LIMITING_AVG"  NB_REGEX 1
  INCLUDE_TEMPLATE ON FE_DIM 2D LIST_N_PROCS ${LIST_PROC}
)
